upstream php56-fpm-upstream { server php56-fpm:9000; }
upstream php70-fpm-upstream { server php70-fpm:9000; }
upstream php71-fpm-upstream { server php71-fpm:9000; }
upstream php72-fpm-upstream { server php72-fpm:9000; }
upstream php73-fpm-upstream { server php73-fpm:9000; }
upstream php74-fpm-upstream { server php74-fpm:9000; }
upstream php80-fpm-upstream { server php80-fpm:9000; }
upstream workspace-upstream { server workspace:4200; }
upstream cockroach-upstream { server cockroachdb:8080; }
# upstream gitlab-upstream { server gitlab:8080; }
